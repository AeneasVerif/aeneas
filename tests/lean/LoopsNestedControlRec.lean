-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [loops_nested_control_rec]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow LoopExit Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

namespace loops_nested_control_rec

/-- [loops_nested_control_rec::rec_break_outer]: loop 1:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 1:0-18:5
    Visibility: public -/
@[rust_loop]
def rec_break_outer_loop0_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32 ×
    Std.U32) Unit Unit)
  := do
  if j < 4#u32
  then
    let i1 ← i + j
    if i1 > 3#u32
    then ok (propagatedBreak (max, i, s))
    else
      let s1 ← s + j
      let j1 ← j + 1#u32
      rec_break_outer_loop0_loop0 max i s1 j1
  else ok (normalBreak (max, i, s, j))
partial_fixpoint

/-- [loops_nested_control_rec::rec_break_outer]: loop 0:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 1:0-18:5
    Visibility: public -/
@[rust_loop]
def rec_break_outer_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) :
  Result (Std.U32 × Std.U32)
  := do
  if i < max
  then
    let loop_exit ← rec_break_outer_loop0_loop0 max i s 0#u32
    match loop_exit with
    | normalBreak (max1, i1, s1, _) =>
      let i2 ← i1 + 1#u32
      rec_break_outer_loop0 max1 i2 s1
    | propagatedBreak (_, i1, i2) => ok (i1, i2)
    | propagatedContinue _ => fail panic
    | propagatedReturn _ => fail panic
  else ok (i, s)
partial_fixpoint

/-- [loops_nested_control_rec::rec_break_outer]:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 4:0-21:1
    Visibility: public -/
def rec_break_outer (max : Std.U32) : Result Std.U32 := do
  let (i, s) ← rec_break_outer_loop0 max 0#u32 0#u32
  s + i

/-- [loops_nested_control_rec::rec_continue_outer]: loop 1:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 1:0-40:5
    Visibility: public -/
@[rust_loop]
def rec_continue_outer_loop0_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32 × Std.U32) Unit (Std.U32 ×
    Std.U32 × Std.U32) Unit)
  := do
  if j < 3#u32
  then
    let j1 ← j + 1#u32
    if j1 = 2#u32
    then ok (propagatedContinue (max, i, s))
    else let s1 ← s + j1
         rec_continue_outer_loop0_loop0 max i s1 j1
  else ok (normalBreak (max, i, s, j))
partial_fixpoint

/-- [loops_nested_control_rec::rec_continue_outer]: loop 0:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 1:0-40:5
    Visibility: public -/
@[rust_loop]
def rec_continue_outer_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) : Result Std.U32 := do
  if i < max
  then
    let i1 ← i + 1#u32
    let loop_exit ← rec_continue_outer_loop0_loop0 max i1 s 0#u32
    match loop_exit with
    | normalBreak (max1, i2, s1, _) =>
      let s2 ← s1 + 10#u32
      rec_continue_outer_loop0 max1 i2 s2
    | propagatedBreak _ => fail panic
    | propagatedContinue (i2, i3, i4) => rec_continue_outer_loop0 i2 i3 i4
    | propagatedReturn _ => fail panic
  else ok s
partial_fixpoint

/-- [loops_nested_control_rec::rec_continue_outer]:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 23:0-43:1
    Visibility: public -/
@[reducible]
def rec_continue_outer (max : Std.U32) : Result Std.U32 := do
  rec_continue_outer_loop0 max 0#u32 0#u32

/-- [loops_nested_control_rec::rec_return_nested]: loop 1:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 50:8-60:1
    Visibility: public -/
@[rust_loop]
def rec_return_nested_loop0_loop0
  (max : Std.U32) (i : Std.U32) (j : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32) Unit Unit (Std.U32 × Std.U32))
  := do
  if j < 4#u32
  then
    let i1 ← i + j
    if i1 > 2#u32
    then ok (propagatedReturn (i1, max))
    else let j1 ← j + 1#u32
         rec_return_nested_loop0_loop0 max i j1
  else ok (normalBreak (max, i, j))
partial_fixpoint

/-- [loops_nested_control_rec::rec_return_nested]: loop 0:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 48:4-60:1
    Visibility: public -/
@[rust_loop]
def rec_return_nested_loop0
  (max : Std.U32) (i : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32) Unit Unit (Std.U32 × Std.U32))
  := do
  if i < max
  then
    let loop_exit ← rec_return_nested_loop0_loop0 max i 0#u32
    match loop_exit with
    | normalBreak (max1, i1, _) =>
      let i2 ← i1 + 1#u32
      rec_return_nested_loop0 max1 i2
    | propagatedBreak _ => fail panic
    | propagatedContinue _ => fail panic
    | propagatedReturn (i1, i2) => ok (propagatedReturn (i1, i2))
  else ok (normalBreak (max, i))
partial_fixpoint

/-- [loops_nested_control_rec::rec_return_nested]:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 45:0-60:1
    Visibility: public -/
def rec_return_nested (max : Std.U32) : Result Std.U32 := do
  let loop_exit ← rec_return_nested_loop0 max 0#u32
  match loop_exit with
  | normalBreak (max1, _) => ok max1
  | propagatedBreak _ => fail panic
  | propagatedContinue _ => fail panic
  | propagatedReturn (i, _) => ok i

/-- [loops_nested_control_rec::rec_break_outer_array_update]: loop 1:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 1:0-75:5
    Visibility: public -/
@[rust_loop]
def rec_break_outer_array_update_loop0_loop0
  (values : Array Std.Usize 4#usize) (i : Std.Usize) (j : Std.Usize) :
  Result (LoopExit ((Array Std.Usize 4#usize) × Std.Usize × Std.Usize)
    (Std.Usize × Std.Usize × Unit × (Std.Usize → Array Std.Usize 4#usize)) Unit
    Unit)
  := do
  if j < 4#usize
  then
    let i1 ← i + j
    let (_, index_mut_back) ← Array.index_mut_usize values j
    if j = 2#usize
    then ok (propagatedBreak (i, i1, (), index_mut_back))
    else
      let j1 ← j + 1#usize
      let a := index_mut_back i1
      rec_break_outer_array_update_loop0_loop0 a i j1
  else ok (normalBreak (values, i, j))
partial_fixpoint

/-- [loops_nested_control_rec::rec_break_outer_array_update]: loop 0:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 1:0-75:5
    Visibility: public -/
@[rust_loop]
def rec_break_outer_array_update_loop0
  (values : Array Std.Usize 4#usize) (i : Std.Usize) :
  Result (Array Std.Usize 4#usize)
  := do
  if i < 4#usize
  then
    let loop_exit ← rec_break_outer_array_update_loop0_loop0 values i 0#usize
    match loop_exit with
    | normalBreak (values1, i1, _) =>
      let i2 ← i1 + 1#usize
      rec_break_outer_array_update_loop0 values1 i2
    | propagatedBreak (_, i1, _, back) => ok (back i1)
    | propagatedContinue _ => fail panic
    | propagatedReturn _ => fail panic
  else ok values
partial_fixpoint

/-- [loops_nested_control_rec::rec_break_outer_array_update]:
    Source: 'tests/src/loops-nested-control-rec.rs', lines 62:0-78:1
    Visibility: public -/
@[reducible]
def rec_break_outer_array_update
  (values : Array Std.Usize 4#usize) : Result (Array Std.Usize 4#usize) := do
  rec_break_outer_array_update_loop0 values 0#usize

end loops_nested_control_rec
