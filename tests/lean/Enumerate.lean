-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [enumerate]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

namespace enumerate

/-- [enumerate::test_enumerate_basic]:
    Source: 'tests/src/enumerate.rs', lines 9:0-19:1 -/
def test_enumerate_basic : Result Unit := do
  let s ←
    lift (Array.to_slice (Array.make 3#usize [ 10#u32, 20#u32, 30#u32 ]))
  let i ← core.slice.Slice.iter s
  let it ← core.slice.iter.IteratorSliceIter.enumerate i
  let (o, it1) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) it
  let (i0, x0) ← core.option.Option.unwrap o
  massert (i0 = 0#usize)
  massert (x0 = 10#u32)
  let (o1, it2) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) it1
  let (i1, x1) ← core.option.Option.unwrap o1
  massert (i1 = 1#usize)
  massert (x1 = 20#u32)
  let (o2, it3) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) it2
  let (i2, x2) ← core.option.Option.unwrap o2
  massert (i2 = 2#usize)
  massert (x2 = 30#u32)
  let (o3, _) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) it3
  let b := core.option.Option.is_none o3
  massert b

/- Unit test for [enumerate::test_enumerate_basic] -/
#assert (test_enumerate_basic == ok ())

/-- [enumerate::test_enumerate_empty]:
    Source: 'tests/src/enumerate.rs', lines 23:0-27:1 -/
def test_enumerate_empty : Result Unit := do
  let s ← lift (Array.to_slice (Std.Array.empty Std.U32))
  let i ← core.slice.Slice.iter s
  let it ← core.slice.iter.IteratorSliceIter.enumerate i
  let (o, _) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) it
  let b := core.option.Option.is_none o
  massert b

/- Unit test for [enumerate::test_enumerate_empty] -/
#assert (test_enumerate_empty == ok ())

/-- [enumerate::test_enumerate_single]:
    Source: 'tests/src/enumerate.rs', lines 31:0-37:1 -/
def test_enumerate_single : Result Unit := do
  let s ← lift (Array.to_slice (Array.make 1#usize [ 42#u32 ]))
  let i ← core.slice.Slice.iter s
  let it ← core.slice.iter.IteratorSliceIter.enumerate i
  let (o, it1) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) it
  let (i1, x) ← core.option.Option.unwrap o
  massert (i1 = 0#usize)
  massert (x = 42#u32)
  let (o1, _) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) it1
  let b := core.option.Option.is_none o1
  massert b

/- Unit test for [enumerate::test_enumerate_single] -/
#assert (test_enumerate_single == ok ())

/-- [enumerate::test_enumerate_docs_example]:
    Source: 'tests/src/enumerate.rs', lines 46:0-56:1 -/
def test_enumerate_docs_example : Result Unit := do
  let s ←
    lift (Array.to_slice (Array.make 3#usize [ 97#u32, 98#u32, 99#u32 ]))
  let i ← core.slice.Slice.iter s
  let iter ← core.slice.iter.IteratorSliceIter.enumerate i
  let (o, iter1) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) iter
  let (i0, x0) ← core.option.Option.unwrap o
  massert (i0 = 0#usize)
  massert (x0 = 97#u32)
  let (o1, iter2) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) iter1
  let (i1, x1) ← core.option.Option.unwrap o1
  massert (i1 = 1#usize)
  massert (x1 = 98#u32)
  let (o2, iter3) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) iter2
  let (i2, x2) ← core.option.Option.unwrap o2
  massert (i2 = 2#usize)
  massert (x2 = 99#u32)
  let (o3, _) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U32) iter3
  let b := core.option.Option.is_none o3
  massert b

/- Unit test for [enumerate::test_enumerate_docs_example] -/
#assert (test_enumerate_docs_example == ok ())

end enumerate
