-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [loops_nested_control_backends]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow LoopExit Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

namespace loops_nested_control_backends

/-- [loops_nested_control_backends::backend_break_outer]: loop body 1:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-17:5
    Visibility: public -/
@[rust_loop_body]
def backend_break_outer_loop0_loop0.body
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32) (LoopExit (Std.U32 × Std.U32 ×
    Std.U32 × Std.U32) (Std.U32 × Std.U32 × Std.U32) Unit Unit))
  := do
  if j < 4#u32
  then
    let i1 ← i + j
    if i1 > 3#u32
    then ok (done (propagatedBreak (max, i, s)))
    else let s1 ← s + j
         let j1 ← j + 1#u32
         ok (cont (s1, j1))
  else ok (done (normalBreak (max, i, s, j)))

/-- [loops_nested_control_backends::backend_break_outer]: loop 1:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-17:5
    Visibility: public -/
@[rust_loop]
def backend_break_outer_loop0_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32 ×
    Std.U32) Unit Unit)
  := do
  loop
    (fun (s1, j1) => backend_break_outer_loop0_loop0.body max i s1 j1)
    (s, j)

/-- [loops_nested_control_backends::backend_break_outer]: loop body 0:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-17:5
    Visibility: public -/
@[rust_loop_body]
def backend_break_outer_loop0.body
  (max : Std.U32) (i : Std.U32) (s : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32))
  := do
  if i < max
  then
    let loop_exit ← backend_break_outer_loop0_loop0 max i s 0#u32
    match loop_exit with
    | normalBreak (max1, i1, s1, _) =>
      let i2 ← i1 + 1#u32
      ok (cont (max1, i2, s1))
    | propagatedBreak (_, i1, i2) => ok (done (i1, i2))
    | propagatedContinue _ => fail panic
    | propagatedReturn _ => fail panic
  else ok (done (i, s))

/-- [loops_nested_control_backends::backend_break_outer]: loop 0:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-17:5
    Visibility: public -/
@[rust_loop]
def backend_break_outer_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) :
  Result (Std.U32 × Std.U32)
  := do
  loop
    (fun (max1, i1, s1) => backend_break_outer_loop0.body max1 i1 s1)
    (max, i, s)

/-- [loops_nested_control_backends::backend_break_outer]:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 3:0-20:1
    Visibility: public -/
def backend_break_outer (max : Std.U32) : Result Std.U32 := do
  let (i, s) ← backend_break_outer_loop0 max 0#u32 0#u32
  s + i

/-- [loops_nested_control_backends::backend_continue_outer]: loop body 1:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-39:5
    Visibility: public -/
@[rust_loop_body]
def backend_continue_outer_loop0_loop0.body
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32) (LoopExit (Std.U32 × Std.U32 ×
    Std.U32 × Std.U32) Unit (Std.U32 × Std.U32 × Std.U32) Unit))
  := do
  if j < 3#u32
  then
    let j1 ← j + 1#u32
    if j1 = 2#u32
    then ok (done (propagatedContinue (max, i, s)))
    else let s1 ← s + j1
         ok (cont (s1, j1))
  else ok (done (normalBreak (max, i, s, j)))

/-- [loops_nested_control_backends::backend_continue_outer]: loop 1:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-39:5
    Visibility: public -/
@[rust_loop]
def backend_continue_outer_loop0_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32 × Std.U32) Unit (Std.U32 ×
    Std.U32 × Std.U32) Unit)
  := do
  loop
    (fun (s1, j1) => backend_continue_outer_loop0_loop0.body max i s1 j1)
    (s, j)

/-- [loops_nested_control_backends::backend_continue_outer]: loop body 0:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-39:5
    Visibility: public -/
@[rust_loop_body]
def backend_continue_outer_loop0.body
  (max : Std.U32) (i : Std.U32) (s : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32 × Std.U32) Std.U32)
  := do
  if i < max
  then
    let i1 ← i + 1#u32
    let loop_exit ← backend_continue_outer_loop0_loop0 max i1 s 0#u32
    match loop_exit with
    | normalBreak (max1, i2, s1, _) =>
      let s2 ← s1 + 10#u32
      ok (cont (max1, i2, s2))
    | propagatedBreak _ => fail panic
    | propagatedContinue (i2, i3, i4) => ok (cont (i2, i3, i4))
    | propagatedReturn _ => fail panic
  else ok (done s)

/-- [loops_nested_control_backends::backend_continue_outer]: loop 0:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-39:5
    Visibility: public -/
@[rust_loop]
def backend_continue_outer_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) : Result Std.U32 := do
  loop
    (fun (max1, i1, s1) => backend_continue_outer_loop0.body max1 i1 s1)
    (max, i, s)

/-- [loops_nested_control_backends::backend_continue_outer]:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 22:0-42:1
    Visibility: public -/
@[reducible]
def backend_continue_outer (max : Std.U32) : Result Std.U32 := do
  backend_continue_outer_loop0 max 0#u32 0#u32

/-- [loops_nested_control_backends::backend_return_nested]: loop body 1:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 49:8-59:1
    Visibility: public -/
@[rust_loop_body]
def backend_return_nested_loop0_loop0.body
  (max : Std.U32) (i : Std.U32) (j : Std.U32) :
  Result (ControlFlow Std.U32 (LoopExit (Std.U32 × Std.U32 × Std.U32) Unit Unit
    (Std.U32 × Std.U32)))
  := do
  if j < 4#u32
  then
    let i1 ← i + j
    if i1 > 2#u32
    then ok (done (propagatedReturn (i1, max)))
    else let j1 ← j + 1#u32
         ok (cont j1)
  else ok (done (normalBreak (max, i, j)))

/-- [loops_nested_control_backends::backend_return_nested]: loop 1:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 49:8-59:1
    Visibility: public -/
@[rust_loop]
def backend_return_nested_loop0_loop0
  (max : Std.U32) (i : Std.U32) (j : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32) Unit Unit (Std.U32 × Std.U32))
  := do
  loop
    (fun j1 => backend_return_nested_loop0_loop0.body max i j1)
    j

/-- [loops_nested_control_backends::backend_return_nested]: loop body 0:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 47:4-59:1
    Visibility: public -/
@[rust_loop_body]
def backend_return_nested_loop0.body
  (max : Std.U32) (i : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32) (LoopExit (Std.U32 × Std.U32) Unit
    Unit (Std.U32 × Std.U32)))
  := do
  if i < max
  then
    let loop_exit ← backend_return_nested_loop0_loop0 max i 0#u32
    match loop_exit with
    | normalBreak (max1, i1, _) => let i2 ← i1 + 1#u32
                                   ok (cont (max1, i2))
    | propagatedBreak _ => fail panic
    | propagatedContinue _ => fail panic
    | propagatedReturn (i1, i2) => ok (done (propagatedReturn (i1, i2)))
  else ok (done (normalBreak (max, i)))

/-- [loops_nested_control_backends::backend_return_nested]: loop 0:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 47:4-59:1
    Visibility: public -/
@[rust_loop]
def backend_return_nested_loop0
  (max : Std.U32) (i : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32) Unit Unit (Std.U32 × Std.U32))
  := do
  loop
    (fun (max1, i1) => backend_return_nested_loop0.body max1 i1)
    (max, i)

/-- [loops_nested_control_backends::backend_return_nested]:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 44:0-59:1
    Visibility: public -/
def backend_return_nested (max : Std.U32) : Result Std.U32 := do
  let loop_exit ← backend_return_nested_loop0 max 0#u32
  match loop_exit with
  | normalBreak (max1, _) => ok max1
  | propagatedBreak _ => fail panic
  | propagatedContinue _ => fail panic
  | propagatedReturn (i, _) => ok i

/-- [loops_nested_control_backends::backend_break_two_depths]: loop body 2:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-82:5
    Visibility: public -/
@[rust_loop_body]
def backend_break_two_depths_loop0_loop0_loop0.body
  (max : Std.U32) (i : Std.U32) (j : Std.U32) (s : Std.U32) (k : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32) (LoopExit (Std.U32 × Std.U32 ×
    Std.U32 × Std.U32 × Std.U32) (Sum (Std.U32 × Std.U32 × Std.U32 × Std.U32)
    (Std.U32 × Std.U32 × Std.U32)) Unit Unit))
  := do
  if k < 3#u32
  then
    if max = 0#u32
    then ok (done (propagatedBreak (Sum.left (max, i, s, j))))
    else
      if max = 1#u32
      then ok (done (propagatedBreak (Sum.right (max, i, s))))
      else
        let i1 ← i + j
        let i2 ← i1 + k
        let s1 ← s + i2
        let k1 ← k + 1#u32
        ok (cont (s1, k1))
  else ok (done (normalBreak (max, i, s, j, k)))

/-- [loops_nested_control_backends::backend_break_two_depths]: loop 2:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-82:5
    Visibility: public -/
@[rust_loop]
def backend_break_two_depths_loop0_loop0_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) (k : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32 × Std.U32 × Std.U32) (Sum
    (Std.U32 × Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32 × Std.U32)) Unit
    Unit)
  := do
  loop
    (fun (s1, k1) => backend_break_two_depths_loop0_loop0_loop0.body max i j s1
      k1)
    (s, k)

/-- [loops_nested_control_backends::backend_break_two_depths]: loop body 1:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-82:5
    Visibility: public -/
@[rust_loop_body]
def backend_break_two_depths_loop0_loop0.body
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32 × Std.U32 × Std.U32) (LoopExit
    (Std.U32 × Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32 × Std.U32) Unit
    Unit))
  := do
  if j < 3#u32
  then
    let loop_exit ← backend_break_two_depths_loop0_loop0_loop0 max i s j 0#u32
    match loop_exit with
    | normalBreak (max1, i1, s1, j1, _) =>
      let j2 ← j1 + 1#u32
      ok (cont (max1, i1, s1, j2))
    | propagatedBreak (Sum.left (i1, i2, i3, i4)) =>
      ok (done (normalBreak (i1, i2, i3, i4)))
    | propagatedBreak (Sum.right (i1, i2, i3)) =>
      ok (done (propagatedBreak (i1, i2, i3)))
    | propagatedContinue _ => fail panic
    | propagatedReturn _ => fail panic
  else ok (done (normalBreak (max, i, s, j)))

/-- [loops_nested_control_backends::backend_break_two_depths]: loop 1:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-82:5
    Visibility: public -/
@[rust_loop]
def backend_break_two_depths_loop0_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32 ×
    Std.U32) Unit Unit)
  := do
  loop
    (fun (max1, i1, s1, j1) => backend_break_two_depths_loop0_loop0.body max1
      i1 s1 j1)
    (max, i, s, j)

/-- [loops_nested_control_backends::backend_break_two_depths]: loop body 0:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-82:5
    Visibility: public -/
@[rust_loop_body]
def backend_break_two_depths_loop0.body
  (max : Std.U32) (i : Std.U32) (s : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32))
  := do
  if i < 3#u32
  then
    let loop_exit ← backend_break_two_depths_loop0_loop0 max i s 0#u32
    match loop_exit with
    | normalBreak (max1, i1, s1, _) =>
      let i2 ← i1 + 1#u32
      ok (cont (max1, i2, s1))
    | propagatedBreak (_, i1, i2) => ok (done (i1, i2))
    | propagatedContinue _ => fail panic
    | propagatedReturn _ => fail panic
  else ok (done (i, s))

/-- [loops_nested_control_backends::backend_break_two_depths]: loop 0:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 1:0-82:5
    Visibility: public -/
@[rust_loop]
def backend_break_two_depths_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) :
  Result (Std.U32 × Std.U32)
  := do
  loop
    (fun (max1, i1, s1) => backend_break_two_depths_loop0.body max1 i1 s1)
    (max, i, s)

/-- [loops_nested_control_backends::backend_break_two_depths]:
    Source: 'tests/src/loops-nested-control-backends.rs', lines 61:0-85:1
    Visibility: public -/
def backend_break_two_depths (max : Std.U32) : Result Std.U32 := do
  let (i, s) ← backend_break_two_depths_loop0 max 0#u32 0#u32
  s + i

end loops_nested_control_backends
