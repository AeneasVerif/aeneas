-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [loops_nested_depth_break_control]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow LoopExit Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

namespace loops_nested_depth_break_control

/-- [loops_nested_depth_break_control::break_two_levels]: loop body 2:
    Source: 'tests/src/loops-nested-depth-break-control.rs', lines 1:0-23:5
    Visibility: public -/
@[rust_loop_body]
def break_two_levels_loop0_loop0_loop0.body
  (max : Std.U32) (i : Std.U32) (j : Std.U32) (s : Std.U32) (k : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32) (LoopExit (Std.U32 × Std.U32 ×
    Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32 × Std.U32) Unit Unit))
  := do
  if k < 3#u32
  then
    let i1 ← i + j
    let i2 ← i1 + k
    if i2 > 3#u32
    then ok (done (propagatedBreak (max, i, s)))
    else let s1 ← s + k
         let k1 ← k + 1#u32
         ok (cont (s1, k1))
  else ok (done (normalBreak (max, i, s, j, k)))

/-- [loops_nested_depth_break_control::break_two_levels]: loop 2:
    Source: 'tests/src/loops-nested-depth-break-control.rs', lines 1:0-23:5
    Visibility: public -/
@[rust_loop]
def break_two_levels_loop0_loop0_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) (k : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32 × Std.U32 × Std.U32) (Std.U32 ×
    Std.U32 × Std.U32) Unit Unit)
  := do
  loop
    (fun (s1, k1) => break_two_levels_loop0_loop0_loop0.body max i j s1 k1)
    (s, k)

/-- [loops_nested_depth_break_control::break_two_levels]: loop body 1:
    Source: 'tests/src/loops-nested-depth-break-control.rs', lines 1:0-23:5
    Visibility: public -/
@[rust_loop_body]
def break_two_levels_loop0_loop0.body
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32 × Std.U32 × Std.U32) (LoopExit
    (Std.U32 × Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32 × Std.U32) Unit
    Unit))
  := do
  if j < 3#u32
  then
    let loop_exit ← break_two_levels_loop0_loop0_loop0 max i s j 0#u32
    match loop_exit with
    | normalBreak (max1, i1, s1, j1, _) =>
      let j2 ← j1 + 1#u32
      ok (cont (max1, i1, s1, j2))
    | propagatedBreak (i1, i2, i3) => ok (done (propagatedBreak (i1, i2, i3)))
    | propagatedContinue _ => fail panic
    | propagatedReturn _ => fail panic
  else ok (done (normalBreak (max, i, s, j)))

/-- [loops_nested_depth_break_control::break_two_levels]: loop 1:
    Source: 'tests/src/loops-nested-depth-break-control.rs', lines 1:0-23:5
    Visibility: public -/
@[rust_loop]
def break_two_levels_loop0_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) (j : Std.U32) :
  Result (LoopExit (Std.U32 × Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32 ×
    Std.U32) Unit Unit)
  := do
  loop
    (fun (max1, i1, s1, j1) => break_two_levels_loop0_loop0.body max1 i1 s1 j1)
    (max, i, s, j)

/-- [loops_nested_depth_break_control::break_two_levels]: loop body 0:
    Source: 'tests/src/loops-nested-depth-break-control.rs', lines 1:0-23:5
    Visibility: public -/
@[rust_loop_body]
def break_two_levels_loop0.body
  (max : Std.U32) (i : Std.U32) (s : Std.U32) :
  Result (ControlFlow (Std.U32 × Std.U32 × Std.U32) (Std.U32 × Std.U32))
  := do
  if i < max
  then
    let loop_exit ← break_two_levels_loop0_loop0 max i s 0#u32
    match loop_exit with
    | normalBreak (max1, i1, s1, _) =>
      let i2 ← i1 + 1#u32
      ok (cont (max1, i2, s1))
    | propagatedBreak (_, i1, i2) => ok (done (i1, i2))
    | propagatedContinue _ => fail panic
    | propagatedReturn _ => fail panic
  else ok (done (i, s))

/-- [loops_nested_depth_break_control::break_two_levels]: loop 0:
    Source: 'tests/src/loops-nested-depth-break-control.rs', lines 1:0-23:5
    Visibility: public -/
@[rust_loop]
def break_two_levels_loop0
  (max : Std.U32) (i : Std.U32) (s : Std.U32) :
  Result (Std.U32 × Std.U32)
  := do
  loop
    (fun (max1, i1, s1) => break_two_levels_loop0.body max1 i1 s1)
    (max, i, s)

/-- [loops_nested_depth_break_control::break_two_levels]:
    Source: 'tests/src/loops-nested-depth-break-control.rs', lines 5:0-26:1
    Visibility: public -/
def break_two_levels (max : Std.U32) : Result Std.U32 := do
  let (i, s) ← break_two_levels_loop0 max 0#u32 0#u32
  s + i

end loops_nested_depth_break_control
