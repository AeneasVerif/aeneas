[package]
name = "avl"
version = "0.1.0"
edition = "2021"

# TODO: If we turn this package into a library, building the tests fails.
[[bin]]
name = "avl"
path = "src/avl.rs"

# [lib]
# name = "avl"
# path = "src/avl.rs"